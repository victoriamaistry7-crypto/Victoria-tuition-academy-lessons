x[1])}</div></div></div>`).join('')}</div><div class="routine"><h3>Evening</h3>${r.pm.map((x,i)=>`<div class="routine-step"><div class="num">${i+1}</div><div><b>${esc(x[0])}</b><div class="mini">${esc(x[1])}</div></div></div>`).join('')}</div><div class="card wide-span" style="margin:0"><div class="section-head"><div><h3>Shopping priorities</h3><p>Budget: R${profile.budget} · estimated selected total R${spent.toFixed(0)}</p></div></div>${picks.length?picks.map((x,i)=>`<div class="shop-card"><div class="eyebrow">Priority ${i+1}</div><h4>${esc(x.name)} · R${x.price.toFixed(2)}</h4><div class="why">${esc(x.why)}</div><div class="actions"><a class="btn small" href="${x.url}" target="_blank" rel="noopener">Check ${esc(x.retailer)}</a><button class="btn small analyseLocal" data-local="${catalog.indexOf(x)}">Analyse</button></div></div>`).join(''):'<div class="notice good"><strong>No urgent shopping list</strong>Keep tolerated basics and avoid buying products just to fill every step.</div>'}</div><div class="card" style="margin:0"><h3>What to stop / pause</h3><div class="mini" style="margin-top:7px">${barrierRisk()?'<b>Pause optional strong exfoliants/retinoids until burning or tightness settles.</b><br>':''}Do not start several new actives together. Stop a new product if it causes persistent burning, swelling, blistering or a rapidly worsening rash.</div></div><div class="card" style="margin:0"><h3>Lifestyle focus</h3>${life.map(x=>`<div class="routine-step"><div class="num">•</div><div class="mini">${esc(x)}</div></div>`).join('')}</div><div class="card wide-span" style="margin:0"><h3>4–12 week roadmap</h3><div class="timeline">${timeline.map(x=>`<div class="week"><b>${x[0]}</b><div class="mini">${x[1]}</div></div>`).join('')}</div></div><div class="card wide-span" style="margin:0"><h3>Doctor / pharmacist review</h3>${doctor.map(x=>`<div class="doctor-item">${esc(x)}</div>`).join('')}</div></div>`;$$('.analyseLocal').forEach(b=>b.onclick=()=>analyseLocal(catalog[+b.dataset.local]))}
$('#refreshPlan').onclick=renderPlan;
function ingredientFlags(inci=''){const s=inci.toLowerCase(),out=[];const map=[['benzoyl peroxide','Acne treatment; can be drying/irritating and bleach fabrics.'],['salicylic acid','BHA exfoliant; may help clogged pores but can irritate a stressed barrier.'],['adapalene','Topical retinoid; effective acne treatment but avoid in pregnancy.'],['tretinoin','Prescription retinoid; avoid in pregnancy and use under clinician guidance.'],['retinol','Retinoid-family cosmetic active; irritation risk and pregnancy caution.'],['azelaic acid','Can help acne and post-inflammatory pigment; usually introduced gradually.'],['niacinamide','Barrier/sebum-supporting ingredient for many people, but not everyone tolerates it.'],['fragrance','Potential irritant for fragrance-sensitive skin.'],['alcohol denat','Can feel drying/irritating in some formulas, especially on a stressed barrier.'],['ceramide','Barrier-supporting lipid.'],['glycerin','Humectant that helps hydration.']];map.forEach(([k,d])=>{if(s.includes(k))out.push([k,d])});return out}
function productFit(name,inci,price){const flags=ingredientFlags(inci),reasons=[],cautions=[];if(!profile)return{label:'Complete consultation',cls:'caution',reasons:['Save your skin profile for personalised analysis.'],cautions,flags};const low=(name+' '+inci).toLowerCase();if(profile.concerns.includes('Breakouts / acne')&&(low.includes('benzoyl')||low.includes('salicylic')||low.includes('azelaic')||low.includes('adapalene')))reasons.push('Contains an ingredient commonly used for acne.');if(profile.concerns.includes('Dryness / dehydration')&&(low.includes('glycerin')||low.includes('ceramide')||low.includes('hyaluronic')))reasons.push('Contains hydration/barrier-supporting ingredients.');if(profile.concerns.includes('Dark marks after spots')&&(low.includes('azelaic')||low.includes('niacinamide')||low.includes('spf')||low.includes('uv')))reasons.push('May support the dark-mark strategy.');if(barrierRisk()&&(low.includes('salicylic')||low.includes('retinol')||low.includes('benzoyl')||low.includes('adapalene')||low.includes('tretinoin')))cautions.push('Your answers suggest barrier stress, so this active may be too aggressive right now.');if(pregnancyFlag()&&(low.includes('adapalene')||low.includes('tretinoin')||low.includes('retinol')||low.includes('retinoid')))cautions.push('Retinoid-family ingredient: pregnancy/trying-to-conceive caution — check with a clinician/pharmacist and avoid topical retinoids in pregnancy.');if(profile.bad&&low.includes('niacinamide')&&profile.bad.toLowerCase().includes('niacinamide'))cautions.push('You reported a previous niacinamide problem.');if(price&&profile.budget&&price>profile.budget)cautions.push('This single product is above your selected starter budget.');const label=cautions.length?'Caution':reasons.length?'Potential fit':'Neutral';return{label,cls:cautions.length?'caution':reasons.length?'good':'caution',reasons:reasons.length?reasons:['No strong match to your top concerns detected from the available data.'],cautions,flags}}
function analysisHtml(p){const fit=productFit(p.name,p.inci||'',p.price||0);return `<div class="card"><div class="section-head"><div><div class="eyebrow">Personalised product review</div><h3>${esc(p.name)}</h3><p>${esc(p.brand||'')}</p></div><span class="fit ${fit.cls}">${esc(fit.label)}</span></div><div class="analysis-grid"><div class="analysis-box"><h4>Why it may fit</h4><ul>${fit.reasons.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div class="analysis-box"><h4>Watch-outs</h4>${fit.cautions.length?`<ul>${fit.cautions.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:'<div class="mini">No major profile-specific warning detected. Patch test and introduce one new product at a time.</div>'}</div><div class="analysis-box"><h4>Key ingredients detected</h4>${fit.flags.length?`<ul>${fit.flags.map(x=>`<li><b>${esc(x[0])}</b> — ${esc(x[1])}</li>`).join('')}</ul>`:'<div class="mini">No key ingredient from the built-in rule set was detected.</div>'}</div><div class="analysis-box"><h4>How to use it</h4><div class="mini">Use according to the product label. If it is an active treatment, introduce slowly and avoid adding another strong new active in the same week. Sunscreen belongs in the morning routine.</div></div></div></div>`}
function analyseLocal(x){nav('products');$('#selectedProduct').classList.add('show');$('#selectedProduct').innerHTML=analysisHtml({name:x.name,brand:x.brand,price:x.price,inci:x.actives.join(', ')});$('#selectedProduct').scrollIntoView({behavior:'smooth',block:'start'})}
async function searchProducts(){const q=$('#searchQ').value.trim();if(!q)return;lastSearch=q;$('#searchStatus').textContent='Searching the Open Beauty Facts catalogue…';$('#productGrid').innerHTML='';try{const u='https://world.openbeautyfacts.org/cgi/search.pl?search_terms='+encodeURIComponent(q)+'&search_simple=1&action=process&json=1&page_size=20&page='+searchPage;const r=await fetch(u);if(!r.ok)throw Error('search');const d=await r.json();const arr=(d.products||[]).filter(x=>x.product_name||x.generic_name);$('#searchStatus').textContent=arr.length?`Showing ${arr.length} results. Community records can be incomplete.`:'No results found.';$('#productGrid').innerHTML=arr.map((x,i)=>{const name=x.product_name||x.generic_name||'Unnamed product',img=x.image_front_small_url||x.image_small_url||'',inci=x.ingredients_text||'';return `<div class="product"><div class="pimg">${img?`<img loading="lazy" src="${esc(img)}" alt="">`:'<span class="muted">No image</span>'}</div><div><h4>${esc(name)}</h4><p>${esc(x.brands||'Brand not listed')}</p><div class="tagrow"><span class="tag">${inci?'INCI available':'INCI missing'}</span>${x.categories_tags?.length?'<span class="tag">Beauty product</span>':''}</div><div class="actions"><button class="btn small primary obfAnalyse" data-i="${i}">Analyse</button></div></div></div>`}).join('');$$('.obfAnalyse').forEach(b=>b.onclick=()=>{const x=arr[+b.dataset.i];const p={name:x.product_name||x.generic_name||'Product',brand:x.brands||'',inci:x.ingredients_text||'',image:x.image_front_url||x.image_url||x.image_front_small_url||''};$('#selectedProduct').classList.add('show');$('#selectedProduct').innerHTML=`${p.image?`<div class="product-head"><img src="${esc(p.image)}" alt=""><div><div class="eyebrow">Selected product</div><h3>${esc(p.name)}</h3><div class="muted">${esc(p.brand)}</div></div></div>`:''}${analysisHtml(p)}`;$('#selectedProduct').scrollIntoView({behavior:'smooth',block:'start'})})}catch(e){$('#searchStatus').textContent='Live catalogue search was unavailable. You can still use manual INCI analysis below.'}}
$('#searchBtn').onclick=searchProducts;$('#searchQ').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();searchProducts()}});$('#mAnalyse').onclick=()=>{$('#manualAnalysis').innerHTML=analysisHtml({name:v('#mName')||'Manual product',inci:v('#mInci'),price:+v('#mPrice')||0})};
function answerQuestion(){const q=$('